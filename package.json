{
  "name": "mini-sns-app",
  "version": "1.0.0",
  "description": "Mini SNS Application",
  "private": true,
  "scripts": {
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "lint:css": "stylelint",
    "lint:css:fix": "stylelint --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard-scss": "^13.0.0"
  }
}
